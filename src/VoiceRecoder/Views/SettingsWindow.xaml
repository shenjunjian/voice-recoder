<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="VoiceRecoder.Views.SettingsWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    Title="设置">

    <Window.SystemBackdrop>
        <MicaBackdrop />
    </Window.SystemBackdrop>

    <Grid Padding="24" RowSpacing="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock
            FontSize="20"
            FontWeight="SemiBold"
            Text="设置" />

        <StackPanel Grid.Row="1" Spacing="8">
            <TextBlock Text="识别语言" />
            <TextBox
                x:Name="LanguageTextBox"
                PlaceholderText="zh-CN" />
        </StackPanel>

        <StackPanel Grid.Row="2" Spacing="8">
            <TextBlock Text="Qwen API Key" />
            <PasswordBox
                x:Name="ApiKeyPasswordBox"
                PlaceholderText="使用 Qwen 方案时填写，仅保存在本地" />
        </StackPanel>

        <InfoBar
            x:Name="SettingsInfoBar"
            Grid.Row="3"
            IsOpen="True"
            IsClosable="False"
            Message="识别方案可在托盘菜单中切换。Windows 内置方案需安装中文语音包并开启在线语音识别；Qwen 方案会产生 API 费用。"
            Severity="Informational" />

        <StackPanel
            Grid.Row="5"
            HorizontalAlignment="Right"
            Orientation="Horizontal"
            Spacing="12">
            <Button
                Click="CancelButton_Click"
                Content="取消" />
            <Button
                Click="SaveButton_Click"
                Content="保存"
                Style="{StaticResource AccentButtonStyle}" />
        </StackPanel>
    </Grid>
</Window>
